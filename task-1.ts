function print(name:string, occu:string, wage:number):string {
  return `Hey, ${name} You are an ${occu} and your hourly wage is ${wage} dollars`;
}

// Example usage:
console.log(print("Sam", "Engineer", 25));
// Output: Hey, Sam. You are an engineer and your hourly wage is 25 dollars.
